import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Membership Tiers — Join the Sultanate of Amexem",
  description:
    "Explore Affiliate, Community, General, and Lead membership tiers at the Sultanate of Amexem. Requirements, benefits, and the path to Moorish American membership.",
};

export default function CitizenshipLayout({ children }: { children: React.ReactNode }) {
  return children;
}
